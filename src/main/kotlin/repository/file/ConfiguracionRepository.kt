package repository.file

import java.io.FileReader

 //class ConfiguracionRepository {

   // private val gson = Gson()

    //fun cargar(ruta: String): Configuracion {
        //try {
            //val reader = FileReader(ruta)
            //return gson.fromJson(reader, Configuracion::class.java)
       // } catch (e: Exception) {
            //throw PersistenciaException("Error al cargar configuración desde $ruta", e)
       // }
   // }
//}